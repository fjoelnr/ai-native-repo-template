# Workflow: Bugfix

1. Read `AGENTS.md` and nearest `AGENT.md`.
2. Reproduce failure and define expected behavior.
3. Implement minimal fix in `src/`.
4. Add regression test in `tests/`.
5. Verify no unintended side effects.
6. Summarize cause, fix, and validation.
