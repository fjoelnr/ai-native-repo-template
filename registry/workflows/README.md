# Workflows Registry

Store reusable ANR-compatible workflow definitions here.

Examples:
- feature development workflow
- bugfix workflow
- migration workflow
- startup operating workflows under `registry/workflows/startup-os/`
