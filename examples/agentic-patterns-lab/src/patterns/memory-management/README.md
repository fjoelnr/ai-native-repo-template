# Memory Management Pattern

This directory holds examples that separate short-term working context from longer-lived facts.

Suggested contents:

- short-term interaction memory
- persistent fact storage
- promotion rules from session memory into durable memory
