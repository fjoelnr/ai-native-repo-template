# Planning Pattern

This directory should hold planning examples where the execution path is not fully known in advance.

Suggested contents:

- planner plus executor split
- replanning on failure
- fixed workflow versus planner comparison

Seeded example:

- `goal-planner.js` builds a small plan from a goal and adapts it to constraints
