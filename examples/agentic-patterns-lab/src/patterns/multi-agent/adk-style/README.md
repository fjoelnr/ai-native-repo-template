# ADK-Style Multi-Agent

This example mirrors a coordinator and sub-agent structure:

- a coordinator inspects the task
- specialist agents receive bounded assignments
- outputs are returned as structured events

It is dependency-free and intended to show the delegation pattern rather than a full framework integration.
