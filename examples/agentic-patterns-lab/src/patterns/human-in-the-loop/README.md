# Human-In-The-Loop Pattern

This directory holds examples where agent execution deliberately pauses for human judgment.

Suggested contents:

- approval requests
- rejection and retry flows
- risk-based escalation
- human override logging

Seeded examples:

- `approval-loop.js` demonstrates the lightweight repository-native pattern
- `adk-real/tool_confirmation_demo.py` demonstrates the real ADK tool confirmation flow
