# Pattern Implementations

Store each pattern in its own directory.

Suggested structure:

- `src/patterns/<pattern-name>/README.md`
- `src/patterns/<pattern-name>/<framework>/`
- `tests/evals/<pattern-name>/`

Current seeded patterns with runnable code:

- `routing`
- `planning`
- `memory-management`
- `multi-agent`
- `human-in-the-loop`
- `guardrails`
- `evaluation-and-monitoring`
